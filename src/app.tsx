import * as ReactDOM from "react-dom";

import "./index.css";

function render() {
  ReactDOM.render(
    <h2 className="text-3xl font-bold underline">Hello from React!</h2>,
    document.body
  );
}

render();
